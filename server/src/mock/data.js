/* Creating an empty array of todo list. */
const todo = [];

/* Creating an array of users objects. */
const users = [
  {
    id: 1,
    username: 'admin',
    password: 'admin1234',
  },
];

/* Exporting the todo and users array to be used in other files. */
module.exports = {
  todo,
  users,
};
